class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        int t=m+n;
        for(int i=n-1;i>=0;i--){
           nums1[t-1] = nums2[i];
            t--;
        }
        Arrays.sort(nums1);
    }
}
