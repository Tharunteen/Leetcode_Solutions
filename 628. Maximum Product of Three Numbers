class Solution {
    public int maximumProduct(int[] nums) {
        Arrays.sort(nums);
        int size=nums.length;
        int ans=nums[size-1]*nums[size-2]*nums[size-3];
        int ans1=nums[0]*nums[1]*nums[size-1];
        return ans1>ans? ans1:ans;
    }
}
